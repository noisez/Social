import React from 'react';
import './dialogs.css';

const Dialogs = (props) => {
    return (
        <div className="dialogs">
            Dialogs
        </div>
    )
};

export default Dialogs;